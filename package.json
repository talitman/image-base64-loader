{
  "name": "image-base64-loader",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "babel --out-dir dist src",
    "lint": "eslint .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "image-type": "^4.0.0",
    "mime-types": "^2.1.22",
    "read-chunk": "^3.1.0",
    "sync-request": "^6.1.0"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "files":["dist"],
  "devDependencies": {
    "@babel/core": "^7.4.0",
    "babel-preset-env": "^1.7.0",
    "rimraf": "^2.6.3",
    "eslint": "^5.15.3",
    "eslint-config-kentcdodds": "^14.3.1"
  }
}
